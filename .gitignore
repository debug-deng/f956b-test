# Odin / Samsung firmware source artifacts (recovered from input AP archive).
# Kept locally for evidence chain; too large / proprietary for git.
work/boot.img
work/boot.img.lz4
work/meta-data/

# Samsung opensource kernel source (640MB) is too large for direct git
# push. The build workflow downloads it from the debug-deng/f956b-test
# GitHub release v1 (see KERNEL_SOURCE_NOTE.md). Keep local copies
# gitignored to prevent accidental commits.
work/SM-F956B_16_Opensource/

# Python cache
__pycache__/
*.pyc

# Local tools dir (downloaded vmlinux-to-elf, kept outside work/)
tools/
work/step3_gen_target_h_old.py

tools/
